import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // The Next.js NextConfig type does not include a `port` option.
  // Configure the dev server port via the start script or environment variables instead.
};

export default nextConfig;
